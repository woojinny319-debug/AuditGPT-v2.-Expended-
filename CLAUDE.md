# CLAUDE.md — RAG_Project_Hyen

이 프로젝트에서 Claude가 항상 따라야 할 지침입니다.
(이 파일은 Claude Code가 매 세션 자동 로드하는 특별 파일입니다. 이름을 바꾸면 자동 적용이 중단됩니다.)

---

## 🎓 학습 자료 자동 생성 규칙 (필수)

사용자(GongZen)는 코딩을 Claude에게 위임하되, **핵심 개념·로직·의사결정 능력**을 직접 학습해 역량을 키우는 것을 목표로 합니다. 따라서 다음을 **기본 동작**으로 삼습니다.

### 언제 생성하나
- 의미 있는 작업(새 기능, 파이프라인 변경, 버그 수정, 아키텍처 결정 등)을 **완료할 때마다** 학습 자료를 작성/갱신한다.
- 단순 오타 수정·파일 이동·사소한 설정 변경 등 **학습 가치가 없는 작업은 생략**한다. (자료가 노이즈로 쌓이지 않게)
- 애매하면 "이 작업에서 배울 개념이 있는가?"로 판단한다. 있으면 작성한다.

### 어디에 저장하나
기존 Obsidian 위키 구조를 그대로 따른다.
- **개념 자료**: `Notes/Concepts/<개념명>.md` — 재사용 가능한 기술/개념 단위
- **세션 자료**: `Notes/Sessions/<주제> (YYYY-MM-DD).md` — 그날 작업의 학습 요약 (사용자 선호 파일명 형식: 제목 뒤에 날짜)
- 작성 후 **`Notes/Home.md`의 노트 맵에 `[[위키링크]]`를 추가**해 인덱스를 갱신한다.
- 다른 노트와 관련되면 본문에서 `[[다른 노트 제목]]`으로 적극 링크한다. 파일명을 바꾸면 위키링크도 함께 갱신한다.
- 적절한 태그(`#concept`, `#code`, `#session` 등)를 단다.
- **토픽 분리 원칙**: 같은 문제의 *정정·심화*면 기존 노트에 이어 쓰고, *층위가 다른 새 문제*면 별도 노트로 만들어 상호 링크한다. (예: "영속화" vs "런타임 안정성"은 별도 노트)

### 어떤 구조로 쓰나 (7개 섹션 고정)

```markdown
## 1. 🎯 한눈에
이번 작업에서 무엇을 했고, 전체 RAG 아키텍처의 어느 부분에 해당하는지.

## 2. 🧒 비유로 이해하기
AI를 잘 모르는 중학생에게 설명하듯, 풍부한 비유로 친절하게.

## 3. 🔬 정석 설명
정확한 기술 용어를 사용한 정석적 설명. 핵심 기술의 개념과 로직을 상세히.

## 4. 🤔 왜 이걸 택했나 (의사결정)
선택한 기술 + 고려했던 대안 + 각 대안의 장단점 + 우리가 이걸 고른 이유.

## 5. 🧭 다음에 비슷한 작업 할 때 참고 포인트
유사 작업 재수행 시 팁·주의. 가능하면 "작은 변형 실험" 거리도 제안.

## 6. 🐛 막혔던 지점 / 디버깅 기록
막혔던 부분·원인·해결 과정. (해당 시) 실패·삽질 로그는 가장 값진 자산.

## 7. ✅ 회상 퀴즈 (정답·해설 포함)
이해를 묻는 질문 2~3개. **각 문제마다 상세 해설 필수** — 왜 그 답인지, 틀리기 쉬운 지점까지.
```

### 작성 원칙
- 비유(2번) → 정석(3번) 순서를 반드시 지킨다(이중 부호화 학습 효과).
- 코드를 그대로 베끼지 말고 **로직과 개념** 위주로 설명한다.
- 한국어로 작성한다.
- 작업 후 사용자에게 "어디에 어떤 학습 자료를 남겼는지" 한 줄로 알려준다.

---

## 🏁 벤치마크 측정 워크플로 (모델 개선 시 필수)

고정 5문항으로 개선 전후 성능을 반복 측정한다. 모델을 **의미 있게 개선**했다고 판단되면(검색/프롬프트/수집/청킹 변경 등):

1. `python run_benchmark.py <라벨>` 실행 — 라벨은 *무엇을 바꿨는지*로 짓는다(예: `after_Q3_multicompany`).
2. 생성된 `Notes/Benchmarks/<날짜>_<라벨>.md`의 **"채점 요약"** 블록을 Claude가 채운다: 각 문항 ✅/⚠️/❌ + 한 줄 평 + 종합 + 🔑 핵심 관찰 + 📈 직전 회차 대비 변화.
3. `Notes/Benchmarks/_벤치마크 추적.md`의 추이 표에 **새 행 추가** + 핵심 이력에 "무엇을 바꿔 무엇이 변했나" 한 줄 기록.
4. **고정 5문항(Q1~Q5)의 문구·순서는 함부로 바꾸지 않는다**(회귀 비교 유지). 문항 변경이 필요하면 사용자와 합의 후 추적 표에 변경 시점을 명시한다. (질문 정의는 `run_benchmark.py`의 `BENCHMARK`가 단일 출처)

---

## 📁 프로젝트 개요
- **목적**: K-IFRS 문서 + DART 공시 데이터 기반 RAG 시스템. 공인회계사(CPA)의 핵심감사사항(KAM) 분석 지원.
- **핵심 코드**: `app.py`(Streamlit UI), `rag_engine.py`(RAG 파이프라인), `prompts.py`(프롬프트), `dart_ingest.py`(DART 수집)
- **벡터 DB**: chromadb. ⚠️ **한글 경로에서 HNSW가 깨지므로 DB는 ASCII 경로 `~/rag_chroma_data/chroma_db_v2`에 둔다**(컬렉션: `dart_docs`/`kifrs`/`kam`). 상세: `Notes/Sessions/ChromaDB ...` 노트.
- **데이터 백업**: `dart_data.pkl`(임베딩 포함), `dart_bm25.pkl`(BM25 사이드카).
- **지식 베이스**: `Notes/`(Obsidian 위키, `Home.md`가 인덱스). 벤치마크: `Notes/Benchmarks/`.
