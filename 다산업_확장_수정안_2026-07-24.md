# 다산업 확장 + 할루시네이션 완화 RAG 수정안

> 상태: **제안(코드 미반영)**. 이 문서는 `sources/` 폴더의 전체 자료를 학습시켜 (1) 여러 산업군에 답할 수 있고 (2) 할루시네이션이 없으며 (3) 감사인이 인용할 수 있는 챗봇을 만들기 위한 기술스택·코드 수정 계획이다. (4) 검색 방식은 현행 hybrid(BM25+Semantic RRF) + FlashRank 2-stage 구조를 그대로 유지한다.

---

## 1. 현재 상태 진단

지금 시스템은 사실상 **제약・바이오 전용**으로 하드코딩되어 있다.

| 파일               | 문제                                                                                                                                                                                                     |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `rag_engine.py`  | `KAM_PDFS`가 `samilpwc_biology-kam.pdf` 1개만 가리킴. `sources/`의 `b2c-accounting-guidebook`, `construction-kam`, `energy-utility-guidebook`, `fnb-accounting-tax-finance-guidebook` 4개 PDF는 **전혀 인입되지 않음**. |
| `dart_ingest.py` | `TARGET_COMPANIES`(32개사), `ACCOUNTING_TOPICS`, `SECTION_WHITELIST`가 전부 제약・바이오 R&D 자산화 이슈에 맞춰짐.                                                                                                         |
| `prompts.py`     | `SYSTEM_PROMPT`가 "제약/바이오 회계 및 공시 데이터 전문 AI 자문가"로 정체성이 못박혀 있고, 32개사 리스트가 문자열로 하드코딩됨.                                                                                                                    |
| `app.py`         | UI 캡션이 "제약·바이오 감사 특화"로 고정.                                                                                                                                                                             |

기술스택 자체(Streamlit, LangChain, ChromaDB, OpenAI 임베딩, BM25, FlashRank)는 유지 가능하며 교체가 필요하지 않다. 다만 `requirements.txt`에 `flashrank` 선언이 누락돼 있어(venv엔 설치돼 있으나 재현 불가 상태) 이 부분만 보강이 필요하다.

---

## 2. 핵심 설계 원칙

**산업(industry)을 회사(company)와 동일한 레벨의 1급 메타데이터 축으로 승격**한다. 이미 `DynamicDartRetriever`가 "질의에서 회사명 감지 → `where={"company":...}`로 필터링"하는 패턴을 구현해뒀으므로, 이 패턴을 산업 축에도 그대로 복제하면 하이브리드 검색·리랭킹 아키텍처를 건드리지 않고도 다산업을 지원할 수 있다.

사용자와 합의한 3가지 정책 방향:

1. **DART 공시 데이터도 다산업으로 확장한다** — 건설/에너지·유틸리티/외식/B2C 대상 기업 리스트를 새로 설계.
2. **근거 부족 시 정책은 현행 유지** — 문서에 직접 근거가 없어도 K-IFRS 일반원칙이나 타사 사례로 추론하되 "추론"이라 라벨링하는 방식을 유지한다. 다만 **구체적 수치·조항번호 등 "사실"은 절대 지어내지 않는다**는 경계를 프롬프트에서 더 명확히 한다.
3. **2차 LLM 근거-답변 일치 검증(faithfulness check)은 추가하지 않는다** — 대신 `FlashrankRerank`가 기본 내장 지원하는 `score_threshold` 파라미터(현재 미사용, 기본값 0.0)로 약한 관련성 청크를 컨텍스트에서 원천 배제해 할루시네이션을 줄인다. 새 컴포넌트가 아니라 기존 리랭커의 파라미터 튜닝이므로 "hybrid + flashrank 유지" 요건과 충돌하지 않는다.

---

## 3. 신규 모듈: `industries.py` (단일 진실 공급원)

지금 `rag_engine.py`와 `dart_ingest.py`에 흩어진 회사 리스트・별칭・KAM PDF 경로・DART 수집 토픽/키워드/섹션 화이트리스트를 산업별로 묶어 하나의 레지스트리로 옮긴다.

```python
@dataclass(frozen=True)
class TargetCompany:
    company_name: str
    stock_code: str | None = None
    aliases: tuple[str, ...] = ()

@dataclass(frozen=True)
class Industry:
    key: str                      # "bio" | "construction" | "energy_utility" | "fnb" | "b2c"
    label: str                    # "제약・바이오"
    guidebook_pdfs: tuple[tuple[str, str, str], ...]  # (filename, source_id, doc_type)
    companies: tuple[TargetCompany, ...]
    accounting_topics: tuple[str, ...]
    topic_keywords: dict[str, tuple[str, ...]]
    section_whitelist: tuple[str, ...]

COMMON_KIFRS_PDFS = (...)   # 산업 공통, industry="공통"
INDUSTRIES: dict[str, Industry] = {"bio": Industry(...), "construction": Industry(...), ...}

def detect_industries_and_companies(query: str) -> tuple[set[str], list[str], list[str]]:
    """질의에서 산업/기업을 감지. 회사가 매칭되면 소속 산업도 자동 감지된다."""
```

산업을 추가/변경할 땐 이 파일만 고치면 `rag_engine.py`, `dart_ingest.py`, `prompts.py`가 자동으로 반영되도록 설계한다.

### 3-1. 5개 산업 매핑 (`sources/` 폴더 기준)

| industry key | 라벨 | guidebook PDF | doc_type |
|---|---|---|---|
| bio | 제약・바이오 | samilpwc_biology-kam.pdf | kam |
| construction | 건설 | samilpwc_construction-kam.pdf | kam |
| energy_utility | 에너지・유틸리티 | samilpwc_energy-utility-guidebook.pdf | guidebook |
| fnb | 외식・식음료 | samilpwc_fnb-accounting-tax-finance-guidebook.pdf | guidebook |
| b2c | 소비재・유통(B2C) | samilpwc_b2c-accounting-guidebook.pdf | guidebook |

### 3-2. 신규 4개 산업의 DART 대상 기업 (초안 — 실행 전 검토/조정 권장)

회사명만 등록(종목코드는 선택사항 — DART `corpCode` API가 이름으로 매칭):

- **건설**: 삼성물산, 현대건설, GS건설, DL이앤씨, 대우건설, 삼성엔지니어링, HDC현대산업개발, 태영건설, 코오롱글로벌, 계룡건설산업, 한신공영, 동부건설
- **에너지・유틸리티**: 한국전력공사, 한국가스공사, SK이노베이션, GS, S-Oil, 두산에너빌리티, 삼천리, 한국지역난방공사, SK가스, 대성에너지, 서울가스, 경동도시가스
- **외식・식음료**: CJ제일제당, 오뚜기, 농심, 오리온, 롯데웰푸드, 하이트진로, 대상, SPC삼립, 매일유업, 남양유업, 풀무원, 동원F&B
- **소비재・유통(B2C)**: 롯데쇼핑, 신세계, 이마트, 현대백화점, GS리테일, BGF리테일, LG생활건강, 아모레퍼시픽, F&F, 한섬, 코스맥스

### 3-3. 신규 4개 산업의 DART 회계 이슈 (초안)

bio의 `ACCOUNTING_TOPICS`/`TOPIC_KEYWORDS`/`SECTION_WHITELIST` 패턴을 산업별 실무 이슈로 치환:

- **건설**: 공사수익인식(진행기준) / 미청구공사・초과청구공사 / 하자보수충당부채 / PF 우발채무・지급보증
- **에너지・유틸리티**: 유형자산 감가상각・손상 / 정부보조금・배출권 / 규제자산부채(요금규제) / 장기공급계약
- **외식・식음료**: 재고자산평가 / 가맹점(프랜차이즈) 수익인식 / 유형자산손상(매장) / 원재료가격변동충당부채
- **소비재・유통**: 수익인식(포인트・리베이트) / 재고자산평가 / 무형자산(브랜드・상표권) / 리스(매장임차)

각 산업의 `section_whitelist`는 bio의 공통 항목("회계정책", "재무상태표", "포괄손익계산서", "현금흐름표", "사업의 개요")을 유지하고 산업 고유 키워드를 추가한다.

---

## 4. `rag_engine.py` 수정 계획

- `K_IFRS_PDFS`/`KAM_PDFS`/`TARGET_COMPANIES_LIST`/`COMPANY_ALIAS_MAP`/`_detect_companies` 하드코딩 제거 → `industries.py`에서 import.
- 컬렉션 `kam` → **`guidebook`으로 개명**, 5개 산업 가이드북 PDF를 전부 인입. 각 `Document.metadata`에 `industry`, `doc_type` 추가(`_load_pdf`에 파라미터 추가). 기존 `get_kifrs_retriever`와 동일한 "컬렉션 없으면 PDF에서 lazy 초기화" 패턴을 유지하므로 **Streamlit 최초 구동 시 자동 인입**된다(별도 스크립트 불필요, 단 최초 로딩이 몇 분 더 걸릴 수 있음).
- `DynamicDartRetriever`와 동일한 구조의 `DynamicGuidebookRetriever` 신설:
  - `industries.detect_industries_and_companies(query)`로 산업 감지.
  - 산업 감지 시 `where={"industry": industry}`로 필터링 하이브리드 검색(기존 `_robust_query` + BM25 RRF 재사용).
  - 여러 산업이 동시 감지(비교 질문)되면 기존 `_interleave` 헬퍼로 산업별 대표성 보장.
  - 산업 미감지 시 기존 `SimpleHybridRetriever`처럼 전역 검색.
- K-IFRS 컬렉션은 산업 필터 없이 전역 검색 유지(모든 산업 공통 기준서). 메타데이터에 `industry="공통"`만 태깅(인용 카드 표시용).
- `get_dart_retriever`: 로직 거의 그대로, 회사 유니버스만 `industries.py`의 통합 리스트로 교체.
- `_build_2stage(base, rerank_top_n, score_threshold=FLASHRANK_SCORE_THRESHOLD)`: `FlashrankRerank(top_n=..., score_threshold=...)`로 파라미터 추가. `score_threshold`는 이미 FlashrankRerank가 지원하는 내장 파라미터(현재 미사용)이므로 컴포넌트 교체 없이 값만 넣으면 된다. 모듈 상단에 조정 가능한 상수로 선언하고(예: `0.02` — 명백히 무관한 꼬리 결과만 걸러내는 보수적 초기값), `run_benchmark.py`로 실측 튜닝.

---

## 5. `dart_ingest.py` 수정 계획

- `TARGET_COMPANIES`/`ACCOUNTING_TOPICS`/`TOPIC_KEYWORDS`/`SECTION_WHITELIST` 하드코딩 제거, `industries.py`에서 가져옴.
- `main()`을 산업 → 기업 이중 루프로 재구성, 각 문서 메타데이터에 `industry` 필드 추가.
- `argparse`로 `--industry <key>` 옵션 추가(예: `python dart_ingest.py --industry construction`). 전체 76개사(기존 32 + 신규 약 44)를 한 번에 돌리면 요청 간 `sleep()` 누적으로 **1.5~3시간 이상** 걸릴 수 있어, 산업 단위로 나눠 순차 실행하는 것이 실무적으로 중요.
- **버그 수정**: `rag_engine._load_dart_bm25_sidecar()`가 `dart_bm25.pkl`을 읽으려 하는데, 현재 `dart_ingest.py`엔 이 파일을 **쓰는 코드가 없다**(주석엔 "ingest가 생성한 사이드카"라 돼 있으나 구현 누락). `main()` 종료 시 전체 인입 문서를 `dart_bm25.pkl`로 pickle 저장하는 코드를 추가한다. DART BM25 코퍼스가 없으면 DART 검색이 semantic-only로 저하되므로, 이건 "hybrid 검색 유지" 요건을 실제로 안정적으로 작동시키기 위한 선결 조건이다.

---

## 6. `prompts.py` 수정 계획

- `SYSTEM_PROMPT` 상수를 `build_system_prompt()` 함수로 변경, `industries.py` 레지스트리에서 산업 목록・산업별 기업 로스터를 런타임에 조립(하드코딩 문자열 제거) → 산업이 늘어나도 프롬프트를 손대지 않아도 됨.
- 정체성 문구를 "제약/바이오 회계 및 공시 데이터 전문"에서 "K-IFRS・DART・KAM/가이드북 기반 다산업 회계감사 전문 AI 자문가"로 일반화.
- 기존 규칙(출처 인용 [S#], 금액 원단위 환산, 연도 명시, 마크다운 포맷)은 그대로 유지.
- **근거 없을 때 추론 허용 규칙(현행 유지)의 경계를 명확히**: "추론이더라도 문서에 없는 구체적인 금액・비율・조항번호・기업 고유 수치를 새로 만들어내지 마라. 추론은 원칙・일반론 수준에 한정하고 반드시 '근거 문서에 직접적인 언급은 없으나...'로 표시하라."
- **산업 교차오염 방지 규칙 신설**: 질의된 기업과 다른 산업의 KAM/가이드북 사례를 그 기업의 사례인 것처럼 인용 금지. 다른 산업 사례를 참고할 땐 반드시 어느 산업・어느 문서의 사례인지 명시.

---

## 7. `app.py` 수정 계획

- `get_kam_retriever` → `get_guidebook_retriever`, 변수명 `kam_retriever` → `guidebook_retriever`로 전체 치환.
- 헤더 캡션 "K-IFRS · DART · KAM | 3-Source RAG | 제약·바이오 감사 특화" → "K-IFRS · DART · KAM/가이드북 | 다산업 감사 지원 RAG"로 교체.
- `prompts.SYSTEM_PROMPT` import를 `prompts.build_system_prompt()` 호출로 교체.
- (선택) 사이드바에 마지막 질의에서 감지된 산업/기업을 작게 표시 — 리트리버가 이미 로그로 찍는 감지 결과를 재사용해 투명성 제공(감사인이 "이 챗봇이 뭘 근거로 찾았는지" 신뢰할 수 있게). 스코프 부담되면 생략 가능.

---

## 8. `requirements.txt`

- `flashrank>=0.2.0` 추가(현재 venv엔 설치돼 있지만 requirements.txt에 선언이 빠져 있어 재현 불가 상태).

---

## 9. 범위 밖 (건드리지 않음)

- `finalize_db.py`, `rebuild_dart_index.py`는 과거 HNSW 장애 복구용 1회성 스크립트로, 지금 쓰이지 않는 구 경로(`chroma_db`, 상대경로 `chroma_db_v2`, 존재하지 않는 `dart_data.pkl`)를 참조한다. 이번 작업과 무관.
- `run_benchmark.py`의 고정 5문항(Q1~Q5)은 CLAUDE.md 규칙상 임의 변경 금지 — 그대로 둔다. 다산업 확장 후 새 산업 벤치마크가 필요하면 별도 합의 후 추가.

---

## 10. 검증 방법 (구현 시)

1. `python dart_ingest.py --industry bio` (기존 검증된 산업으로 먼저 파이프라인 검증) → 이어서 나머지 산업 순차 실행.
2. `streamlit run app.py` 구동 → 최초 로딩 시 `guidebook`/`kifrs` 컬렉션이 lazy 초기화되는지 확인(5개 PDF 임베딩이라 기존보다 오래 걸릴 수 있음).
3. 산업별 스팟체크:
   - 건설: "GS건설의 미청구공사 회계처리 기준과 최근 재무제표상 금액을 알려줘"
   - 에너지: "한국가스공사의 정부보조금 회계처리는?"
   - 교차오염 체크: 바이오 기업 질문에 건설 KAM 사례가 섞여 인용되지 않는지 [S#] 카드로 확인.
   - 근거부족 케이스: 소스에 없는 조합을 물어 "직접적인 언급은 없으나..." 문구는 나오되 없는 수치를 지어내지 않는지 확인.
4. `python run_benchmark.py after_multi_industry` 실행 후 기존 bio 관련 Q1~Q5 답변 품질이 리팩터링으로 퇴행하지 않았는지 회귀 확인, `_벤치마크 추적.md`에 결과 기록(CLAUDE.md 워크플로).
5. `FLASHRANK_SCORE_THRESHOLD` 값을 0(현행)/0.05/0.1 단계로 바꿔가며 근거 카드 개수・품질 변화를 비교해 최종값 확정.

---

## 11. 예상 비용/시간

- guidebook 5개 PDF(총 361페이지) 임베딩: OpenAI `text-embedding-3-small` 기준 비용은 미미하나, 기존 배치 지연(`EMBED_BATCH_DELAY=1.6s`) 때문에 최초 로딩에 수 분 소요.
- DART 76개사(기존 32 + 신규 약 44) 전체 수집: 회사・보고서・섹션 단위 `sleep()` 누적으로 **1.5~3시간 이상**. `--industry` 단위로 나눠 실행 권장.

---

## 12. 실행 여부 확인이 필요한 지점

- 신규 4개 산업의 DART 대상 기업 리스트(3-2)는 초안이므로, 실제 구현 전에 **회사 목록 확정 필요**(추가/제외 희망 기업 있는지).
- `FLASHRANK_SCORE_THRESHOLD` 초기값(0.02)은 미검증 추정치 — 실측 튜닝 전까지는 보수적으로 두는 것을 권장.
